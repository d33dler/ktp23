version: '3.8'

services:

  backend:
    build: ./backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    ports:
      - "80:5173"
    depends_on:
      - backend

volumes:
  caddy-data:
  caddy-config:
